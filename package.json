{
  "name": "tauri-test",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "cd front && pnpm run dev && cd ..",
    "build": "cd front && pnpm run build && cd ..",
    "start": "cd front && pnpm run start && cd ..",
    "preview": "cd front && pnpm run preview && cd ..",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-cli": "~2",
    "@tauri-apps/plugin-opener": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "typescript": "~5.6.2",
    "vite": "^6.0.3"
  }
}
